import { useState } from "react";
import { ArrowLeft, BadgeCheck, Camera, Check, CheckCircle2, ChevronRight, FileText, Heart, MapPin, MessageCircle, Share2, Sparkles, Star, Upload } from "lucide-react";
import { useNavigate, useParams } from "react-router";

const vendor = {
  name: "블루밍 스튜디오",
  category: "스튜디오 · 강남구",
  price: "80–150만원",
  image: "https://images.unsplash.com/photo-1606216794074-735e91aa2c92?w=900&h=640&fit=crop&auto=format",
};

function BackHeader({ title, fallback = "/" }: { title: string; fallback?: string }) {
  const navigate = useNavigate();
  const goBack = () => window.history.length > 1 ? navigate(-1) : navigate(fallback);
  return <header className="sticky top-0 z-30 flex h-14 items-center border-b border-border/70 bg-background/95 px-4 backdrop-blur">
    <button onClick={goBack} aria-label="이전 화면으로" className="grid h-10 w-10 place-items-center rounded-full transition-colors hover:bg-secondary"><ArrowLeft className="h-5 w-5" /></button>
    <p className="flex-1 pr-10 text-center text-sm font-bold">{title}</p>
  </header>;
}

export function Login() {
  const navigate = useNavigate();
  return <div className="min-h-[100dvh] bg-background px-6 pb-8 pt-16">
    <div className="mb-auto">
      <p className="font-mono text-[10px] tracking-[0.24em] text-primary">WEDDING, LESS OVERWHELMING</p>
      <h1 className="mt-5 font-display text-[42px] font-bold leading-[1.06] tracking-[-0.025em]">결정은 가볍게,<br />준비는 단단하게.</h1>
      <p className="mt-5 max-w-[300px] text-[15px] leading-7 text-muted-foreground">흩어진 웨딩 정보를 한곳에 모아, 우리에게 맞는 선택만 남겨드릴게요.</p>
      <div className="mt-12 rounded-[28px] border border-primary/15 bg-primary/[0.06] p-5">
        <div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-primary text-lg text-white">✦</span><div><p className="text-sm font-bold">나에게 맞는 순서부터</p><p className="mt-0.5 text-xs text-muted-foreground">예산, 지역, 날짜를 기준으로 시작해요</p></div></div>
      </div>
    </div>
    <div className="pt-10">
      <button onClick={() => navigate("/onboarding")} className="flex h-14 w-full items-center justify-center gap-2 rounded-2xl bg-[#FEE500] text-[15px] font-bold text-[#191600] transition-transform active:scale-[0.98]"><span className="grid h-5 w-5 place-items-center rounded-md bg-[#191600] text-[10px] font-black text-[#FEE500]">k</span>카카오로 3초 만에 시작하기</button>
      <p className="mt-4 text-center text-[11px] leading-5 text-muted-foreground">시작하면 웨딩픽 이용약관과 개인정보 처리방침에 동의하게 됩니다.</p>
    </div>
  </div>;
}

const steps = [{ eyebrow: "01 / 03", title: "결혼 예정일이 있나요?", sub: "아직 정하지 않았어도 괜찮아요.", choices: ["2027년 1월 15일", "2027년 상반기", "아직 미정"] }, { eyebrow: "02 / 03", title: "어디에서 준비할까요?", sub: "업체 거리와 실제 견적을 더 정확히 보여드려요.", choices: ["서울", "경기·인천", "다른 지역"] }, { eyebrow: "03 / 03", title: "무엇이 가장 중요해요?", sub: "추천 순서를 조정할 때만 사용할게요.", choices: ["예산 안에서", "취향이 뚜렷하게", "후기가 확실하게"] }];

export function Onboarding() {
  const [step, setStep] = useState(0); const [selected, setSelected] = useState(0); const navigate = useNavigate(); const current = steps[step];
  const next = () => step === steps.length - 1 ? navigate("/") : (setStep(step + 1), setSelected(0));
  return <div className="min-h-[100dvh] bg-background px-6 pb-8 pt-8"><div className="flex items-center justify-between"><button onClick={() => step ? setStep(step - 1) : navigate("/login")} className="text-sm text-muted-foreground">{step ? "이전" : "나중에"}</button><span className="font-mono text-xs text-muted-foreground">{current.eyebrow}</span></div><div className="mt-5 flex gap-1.5">{steps.map((_, i) => <span key={i} className={`h-1 flex-1 rounded-full ${i <= step ? "bg-primary" : "bg-secondary"}`} />)}</div><div className="pt-20"><p className="font-mono text-[10px] tracking-[0.22em] text-primary">JUST FOR YOU</p><h1 className="mt-4 font-display text-[38px] font-bold leading-[1.18] tracking-[-0.025em]">{current.title}</h1><p className="mt-3 text-sm text-muted-foreground">{current.sub}</p><div className="mt-10 space-y-3">{current.choices.map((choice, i) => <button key={choice} onClick={() => setSelected(i)} className={`flex w-full items-center justify-between rounded-2xl border p-5 text-left text-[15px] font-bold transition-colors ${selected === i ? "border-primary bg-primary/[0.07] text-primary" : "border-border bg-card"}`}><span>{choice}</span>{selected === i && <Check className="h-5 w-5" />}</button>)}</div></div><button onClick={next} className="mt-10 flex h-14 w-full items-center justify-center gap-2 rounded-2xl bg-foreground text-sm font-bold text-white">{step === 2 ? "나만의 준비 시작하기" : "다음"}<ChevronRight className="h-4 w-4" /></button></div>;
}

export function VendorDetail() {
  const { id } = useParams(); const navigate = useNavigate(); const [picked, setPicked] = useState(false);
  return <div className="pb-8"><BackHeader title="업체 정보" fallback="/explore" /><div className="relative"><img src={vendor.image} alt="창가 자연광이 들어오는 블루밍 스튜디오" className="h-[280px] w-full object-cover bg-muted" /><button onClick={() => setPicked(!picked)} className={`absolute right-5 top-5 grid h-10 w-10 place-items-center rounded-full backdrop-blur ${picked ? "bg-primary text-white" : "bg-white/90 text-foreground"}`}><Heart className={`h-5 w-5 ${picked ? "fill-current" : ""}`} /></button></div><main className="px-5"><div className="mt-6 flex items-start justify-between"><div><p className="text-xs font-bold tracking-wide text-primary">{vendor.category}</p><h1 className="mt-1 font-display text-3xl font-bold tracking-[-0.02em]">{vendor.name}</h1></div><button className="mt-1 text-muted-foreground" aria-label="공유"><Share2 className="h-5 w-5" /></button></div><div className="mt-3 flex flex-wrap items-center gap-2 text-sm"><span className="inline-flex items-center gap-1 font-bold"><Star className="h-4 w-4 fill-[#BA7A3A] text-[#BA7A3A]" />4.9</span><span className="text-muted-foreground">후기 198개</span><span className="text-muted-foreground">·</span><span className="inline-flex items-center gap-1 text-muted-foreground"><MapPin className="h-3.5 w-3.5" />서울 강남구</span></div><section className="mt-6 rounded-2xl bg-foreground p-5 text-white"><p className="font-mono text-[10px] tracking-[0.15em] text-white/45">VERIFIED PRICE RANGE</p><div className="mt-2 flex items-end justify-between"><p className="font-mono text-2xl font-medium">{vendor.price}</p><span className="inline-flex items-center gap-1 text-xs text-white/75"><BadgeCheck className="h-4 w-4 text-[#D9A47D]" />최근 30일 24건</span></div><p className="mt-3 text-xs leading-5 text-white/60">실제 계약 인증과 업체 공식 견적을 함께 반영했어요.</p></section><section className="mt-7"><div className="flex items-center justify-between"><h2 className="text-base font-bold">왜 추천했어요?</h2><span className="text-xs text-primary">내 조건 기준</span></div><div className="mt-3 grid grid-cols-3 gap-2">{["강남 30분 이내", "자연광 취향", "예산 안쪽"].map((text) => <div key={text} className="rounded-xl bg-secondary px-3 py-3 text-center text-[11px] font-semibold leading-4">{text}</div>)}</div></section><section className="mt-7 border-t border-border pt-6"><div className="flex items-center justify-between"><h2 className="text-base font-bold">진짜 후기 <span className="text-primary">198</span></h2><button onClick={() => navigate("/community")} className="text-xs font-bold text-primary">커뮤니티에서 보기</button></div><article className="mt-4 rounded-2xl border border-border p-4"><div className="flex items-center gap-2 text-xs"><span className="grid h-7 w-7 place-items-center rounded-full bg-secondary font-bold text-primary">윤</span><span className="font-bold">윤슬부부</span><span className="text-muted-foreground">2026.08 계약 인증</span></div><p className="mt-3 text-sm leading-6">작가님이 저희 표정을 편하게 이끌어주셨어요. 추가 비용도 상담 때 들은 범위에서 마무리됐습니다.</p></article></section></main><div className="sticky bottom-0 mt-8 border-t border-border bg-background/95 p-4 backdrop-blur"><div className="mx-auto flex max-w-[430px] gap-2"><button onClick={() => setPicked(!picked)} className="h-14 flex-1 rounded-xl border border-border text-sm font-bold">{picked ? "Pick에 저장됨" : "Pick에 저장"}</button><button onClick={() => navigate(`/our-wedding?visit=${id ?? "2"}`)} className="h-14 flex-1 rounded-xl bg-primary text-sm font-bold text-white">상담 일정 잡기</button></div></div></div>;
}

export function ContractVerify() {
  const [fileName, setFileName] = useState("");
  const [status, setStatus] = useState<"idle" | "reading" | "done">("idle");
  const handleFile = (file?: File) => {
    if (!file) return;
    setFileName(file.name); setStatus("reading");
    window.setTimeout(() => setStatus("done"), 900);
  };
  return <div className="min-h-[100dvh] bg-background"><BackHeader title="실계약 인증" fallback="/pick" /><main className="px-5 pb-8 pt-7"><p className="font-mono text-[10px] tracking-[0.2em] text-primary">VERIFY THE FACTS</p><h1 className="mt-2 font-display text-3xl font-bold tracking-[-0.02em]">계약 내용을<br />자동으로 정리해드릴게요.</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">견적서 또는 영수증 사진을 올리면 금액과 업체 정보를 읽어 Pick과 예산에 반영해요.</p><label className="mt-7 flex min-h-48 cursor-pointer flex-col items-center justify-center rounded-3xl border border-dashed border-primary/45 bg-secondary/50 px-5 text-center"><input type="file" accept="image/*,.pdf" className="sr-only" onChange={(event) => handleFile(event.target.files?.[0])} />{status === "idle" ? <><span className="grid h-12 w-12 place-items-center rounded-2xl bg-white text-primary shadow-sm"><Camera className="h-5 w-5" /></span><p className="mt-4 text-sm font-bold text-foreground">견적서 · 영수증 사진 올리기</p><p className="mt-1 text-xs text-muted-foreground">JPG, PNG, PDF · 최대 10MB</p></> : status === "reading" ? <><span className="grid h-12 w-12 place-items-center rounded-2xl bg-primary text-white"><Upload className="h-5 w-5 animate-bounce" /></span><p className="mt-4 text-sm font-bold text-foreground">내용을 읽고 있어요</p><p className="mt-1 text-xs text-muted-foreground">사진이 선명할수록 정확해요</p></> : <><span className="grid h-12 w-12 place-items-center rounded-2xl bg-primary text-white"><CheckCircle2 className="h-5 w-5" /></span><p className="mt-4 text-sm font-bold text-foreground">입력할 내용을 찾았어요</p><p className="mt-1 max-w-full truncate text-xs text-muted-foreground">{fileName}</p></>}</label>{status === "done" && <section className="mt-5 rounded-2xl border border-border bg-card p-4"><div className="flex items-center gap-2"><FileText className="h-4 w-4 text-primary" /><p className="text-sm font-bold">추출된 계약 정보</p><span className="ml-auto rounded-full bg-primary/10 px-2 py-1 text-[10px] font-bold text-primary">검토 필요</span></div><dl className="mt-4 space-y-3 text-sm"><div className="flex justify-between"><dt className="text-muted-foreground">업체</dt><dd className="font-bold">블루밍 스튜디오</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">계약 금액</dt><dd className="font-mono font-bold">1,280,000원</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">계약일</dt><dd className="font-mono font-bold">2026.09.13</dd></div></dl><button className="mt-5 h-12 w-full rounded-xl bg-primary text-sm font-bold text-white">확인 후 예산에 반영하기</button></section>}<p className="mt-5 text-center text-[11px] leading-5 text-muted-foreground">인증 자료는 계약 확인 목적으로만 사용되며, 반영 전 내용을 직접 확인할 수 있어요.</p></main></div>;
}

export function CommunityFeed() {
  const [liked, setLiked] = useState(false);
  return <div className="pb-5"><header className="sticky top-0 z-20 flex h-14 items-center justify-between border-b border-border/70 bg-background/95 px-5 backdrop-blur"><p className="font-display text-2xl font-bold tracking-[-0.03em]">weddi</p><div className="flex gap-3"><button aria-label="알림" className="text-foreground"><Heart className="h-5 w-5" /></button><button aria-label="메시지" className="text-foreground"><MessageCircle className="h-5 w-5" /></button></div></header><main><div className="flex gap-4 overflow-x-auto border-b border-border px-5 py-4">{[{ name: "내 이야기", tone: "bg-foreground" }, { name: "계약 인증", tone: "bg-primary" }, { name: "예산 고민", tone: "bg-[#D49A70]" }, { name: "드레스", tone: "bg-[#B1B0EB]" }, { name: "신혼집", tone: "bg-[#A8D5CC]" }].map((story) => <button key={story.name} className="flex w-14 flex-none flex-col items-center gap-1.5"><span className="rounded-full border-2 border-primary p-0.5"><span className={`grid h-11 w-11 place-items-center rounded-full ${story.tone} text-xs font-bold text-white`}>{story.name.slice(0, 1)}</span></span><span className="w-full truncate text-[10px] text-muted-foreground">{story.name}</span></button>)}</div><div className="flex gap-2 overflow-x-auto px-5 py-4">{["전체", "계약 후기", "예산", "스드메", "일정"].map((chip, i) => <button key={chip} className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold ${i === 0 ? "bg-foreground text-white" : "bg-secondary text-muted-foreground"}`}>{chip}</button>)}</div><article className="border-y border-border bg-card"><div className="flex items-center justify-between p-4"><div className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-full bg-primary/15 text-sm font-bold text-primary">수</span><div><p className="text-sm font-bold">수아와 도윤 <span className="ml-1 text-[10px] font-medium text-muted-foreground">2시간</span></p><p className="text-[11px] text-muted-foreground">블루밍 스튜디오 · 계약 인증</p></div></div><span className="text-lg text-muted-foreground">•••</span></div><img src={vendor.image} alt="커뮤니티 사용자가 공유한 스튜디오 촬영 공간" className="h-[300px] w-full object-cover" /><div className="p-4"><div className="flex items-center justify-between"><div className="flex gap-4"><button onClick={() => setLiked(!liked)} aria-label="도움돼요" className={liked ? "text-primary" : "text-foreground"}><Heart className={`h-6 w-6 ${liked ? "fill-current" : ""}`} /></button><button aria-label="댓글"><MessageCircle className="h-6 w-6" /></button></div><button aria-label="저장"><Sparkles className="h-5 w-5" /></button></div><p className="mt-3 text-xs font-bold">도움돼요 42개</p><p className="mt-2 text-sm leading-6"><span className="mr-1 font-bold">수아와 도윤</span>세 곳을 비교하고 최종 결정했어요. 최종 견적도 웨딩픽에서 본 범위 안이었습니다.</p><button className="mt-2 text-xs text-muted-foreground">댓글 8개 모두 보기</button></div></article><button className="mx-5 mt-4 flex w-[calc(100%-2.5rem)] items-center justify-center gap-2 rounded-2xl border border-dashed border-primary/45 bg-primary/[0.04] p-4 text-sm font-bold text-primary"><Sparkles className="h-4 w-4" />나의 결정도 기록하기</button></main></div>;
}
